/**
 * Configuration module for CBEnvVar
 */
component {

    this['moduleMapping'] = 'cbengvar'; // cflint ignore:line
    this['modelNamespace'] = 'cbengvar'; // cflint ignore:line
    this['cfmapping'] = 'cbengvar'; // cflint ignore:line

    /**
     * The minimum functions needed to run ModuleConfig
     */
    void function configure() {
    }

}

