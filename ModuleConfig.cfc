/**
 * Configuration module for CBEnvVar
 */
component {

    this['moduleMapping'] = 'cbeditenv'; // cflint ignore:line
    this['modelNamespace'] = 'cbeditenv'; // cflint ignore:line
    this['cfmapping'] = 'cbeditenv'; // cflint ignore:line

    /**
     * The minimum functions needed to run ModuleConfig
     */
    void function configure() {
    }

}

